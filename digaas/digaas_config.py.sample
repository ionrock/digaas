# SAMPLE CONFIG FILE: copy this file to digaas_config.py

# select the storage module (only option is storage_redis)
import storage_redis as storage

redis_host = None
redis_port = None
redis_password = None

# the timeout in seconds for raw dns queries
dns_query_timeout = 0.5
